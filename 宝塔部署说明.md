# 宝塔面板部署量化机器人（进程守护）

本项目是 **FastAPI 后端 + 已打包前端静态文件**，服务器上只需跑 Python，不必再装 Node。

推荐目录：`/www/wwwroot/quant-bot`

代码仓库：[https://github.com/ryf13719652107-bot/AIO](https://github.com/ryf13719652107-bot/AIO)

---

## 〇、拉取代码（推荐，置顶）

在服务器上直接从 GitHub 克隆，比上传 zip 更方便后续更新：

```bash
# 首次部署：克隆到推荐目录
cd /www/wwwroot
git clone https://github.com/ryf13719652107-bot/AIO.git quant-bot
cd quant-bot
```

```bash
# 已有目录时更新到最新代码（保留 backend/.env 与 backend/data/）
cd /www/wwwroot/quant-bot
git pull
```

克隆完成后，从本文 **「三、服务器安装依赖」** 继续；若已安装过，更新后在宝塔进程守护里点 **重启**。

本机开发拉取：

```bash
git clone https://github.com/ryf13719652107-bot/AIO.git
cd AIO
```

---

## 一、本机准备上传包（可选，不用 Git 时）

1. 桌面已生成（或按下面命令打包）：`量化机器人-部署包.zip`
2. **不要**把本机 `.env` 里的密钥明文发到不安全渠道；上传后在服务器单独填写 API Key。
3. 包内已含 `backend/static`（前端），服务器一般不用再 `npm build`。

---

## 二、上传到宝塔

### 方式 A：宝塔文件管理（最简单）

1. 登录宝塔面板
2. **文件** → 进入 `/www/wwwroot/`
3. 新建目录 `quant-bot`
4. 上传 `量化机器人-部署包.zip` 到该目录
5. 右键 zip → **解压**
6. 解压后目录结构应类似：

```text
/www/wwwroot/quant-bot/
  backend/
    app/
    static/
    start.sh
    requirements.txt
    .env.example
  deploy/
    install.sh
  策略逻辑
```

### 方式 B：SSH 上传

```bash
# 本机（PowerShell / 有 scp 时）
scp 量化机器人-部署包.zip root@你的服务器IP:/www/wwwroot/

# 服务器上
cd /www/wwwroot
mkdir -p quant-bot
unzip -o 量化机器人-部署包.zip -d quant-bot
cd quant-bot
```

### 方式 C：Git 克隆（与文首「〇、拉取代码」相同）

```bash
cd /www/wwwroot
git clone https://github.com/ryf13719652107-bot/AIO.git quant-bot
cd quant-bot
```

---

## 三、服务器安装依赖

SSH 或宝塔「终端」执行：

```bash
cd /www/wwwroot/quant-bot
bash deploy/install.sh
```

若提示没有 python3：

- 宝塔 → **软件商店** → 搜索 **Python 项目管理器** / 安装 Python 3.10+
- 或：`yum install -y python3 python3-pip`（CentOS）  
  `apt install -y python3 python3-venv python3-pip`（Ubuntu/Debian）

---

## 四、编辑配置 `.env`

```bash
nano /www/wwwroot/quant-bot/backend/.env
```

**必须改：**

| 项 | 说明 |
|----|------|
| `JWT_SECRET` | 随机长字符串（install.sh 可能已生成） |
| `ADMIN_PASSWORD` | 后台登录密码，勿用默认 |
| `BINANCE_API_KEY` / `SECRET` | 可先空着，登录后在网页「交易所设置」填写亦可 |
| `PAPER_TRADING` | 首次建议 `true`（模拟）；确认无误再改 `false` 或网页改 |
| `APP_PORT` | 默认 `3003` |

保存退出：`Ctrl+O` Enter，`Ctrl+X`。

---

## 五、宝塔「进程守护」启动（重点）

1. 宝塔 → **软件商店** → 安装 **Supervisor管理器**（或「进程守护管理器」）
2. 打开 → **添加守护进程**

填写示例：

| 字段 | 值 |
|------|-----|
| 名称 | `quant-bot` |
| 启动用户 | `root` |
| 运行目录 | `/www/wwwroot/quant-bot/backend` |
| 启动命令 | `/www/wwwroot/quant-bot/backend/start.sh` |

若 `start.sh` 无执行权限：

```bash
chmod +x /www/wwwroot/quant-bot/backend/start.sh
```

**备选启动命令**（不依赖 start.sh）：

```text
/www/wwwroot/quant-bot/.venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 3003
```

3. 保存后点 **启动**，状态应为「运行中」
4. 看日志是否有 `Application startup complete` / `Uvicorn running`

---

## 六、放行端口

1. 宝塔 → **安全** → 放行 `3003`（或你的 `APP_PORT`）
2. 云厂商控制台（阿里云/腾讯云等）**安全组**同样放行该端口入站 TCP

---

## 七、访问与使用

1. 浏览器打开：`http://服务器公网IP:3003`
2. 登录：`.env` 里的 `ADMIN_USERNAME` / `ADMIN_PASSWORD`
3. **交易所设置**：填币安 API（权限勾合约，勿勾提现；IP 白名单填服务器 IP）
4. 先 **模拟盘** 跑通，再改实盘
5. 点 **启动策略**

健康检查：`http://服务器IP:3003/api/health` 应返回 `{"status":"ok"}`

---

## 八、可选：用域名 + Nginx 反代

宝塔 → 网站 → 添加站点（例如 `quant.你的域名.com`）→ 设置 → 反向代理：

- 目标 URL：`http://127.0.0.1:3003`
- 开启 WebSocket 支持（若面板有该选项务必打开；否则实时行情/日志可能断）

Nginx 额外参考（按需粘贴到站点配置）：

```nginx
location / {
    proxy_pass http://127.0.0.1:3003;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 86400;
}
```

然后可只放行 80/443，不必对公网暴露 3003。

---

## 九、更新代码

### 推荐：Git 拉取

```bash
cd /www/wwwroot/quant-bot
git pull
```

然后在宝塔进程守护里对该进程点 **重启**。若改了 `requirements.txt`，先：

```bash
cd /www/wwwroot/quant-bot
source .venv/bin/activate
pip install -r backend/requirements.txt
```

### 备选：重新上传 zip

1. 本机改完后重新打包上传覆盖（保留服务器上的 `backend/.env` 和 `backend/data/`）
2. 宝塔进程守护里对该进程点 **重启**

---

## 十、常见问题

| 现象 | 处理 |
|------|------|
| 启动失败 / 连接币安超时 | 服务器需能访问 `fapi.binance.com`；海外机更稳，国内机可能要代理 |
| 页面打不开 | 查进程是否运行、端口是否放行、安全组 |
| 登录后 WS 断 | Nginx 未开 WebSocket；或用 IP:端口直连测试 |
| 权限错误 | `chown -R www:www /www/wwwroot/quant-bot` 或启动用户改 root |
| 想停策略 | 网页点停止；想停进程：守护进程里点停止 |

---

## 重要提醒

- API Key **不要勾选提现**
- 币安 API 白名单加上 **服务器公网 IP**
- 先用模拟盘验证策略与止盈止损，再开实盘
- `backend/data/app.db` 含交易与配置，备份时一并备份
- 仓库不含真实 `.env`；服务器上请从 `.env.example` 复制并填写

如需我远程代配：请提供服务器 **IP、SSH 账号密码或密钥**（可改临时密码），我才能直接帮你上传安装。无 SSH 时按上文用宝塔文件管理或 Git 克隆即可完成。
